#version 320 es

precision highp float;

precision highp int;

void main()
{
 vec2 pos = vec2(1.0);
 vec3 donor_replacementGLF_dead17vecCoor = vec3(1.0);
 donor_replacementGLF_dead17vecCoor = vec3(1.0, 3.8, 1.0);
 pos = clamp(donor_replacementGLF_dead17vecCoor.yx - 1.0, 1.0, 1.0);
 if(pos.x < 1.0)
  {
  }
}
